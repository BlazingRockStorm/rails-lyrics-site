version: '3'

services:
  web:
    build: .
    command: bash -c "rm -f tmp/pids/server.pid && bundle exec rails assets:precompile && bundle exec puma -C config/puma/production.rb -e production"
    volumes:
      - .:/rails-lyrics-site
      - /rails-lyrics-site/node_modules
      - bundle:/usr/local/bundle
      - log-data:/rails-lyrics-site/log
    ports:
      - "3000:3000"

  nginx:
    build: nginx
    volumes:
      - .:/rails-lyrics-site
    ports:
      - "80:80"
    depends_on:
      - web

volumes:
  bundle:
  log-data: